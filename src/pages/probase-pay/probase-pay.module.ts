import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ProbasePayPage } from './probase-pay';

@NgModule({
  declarations: [
    ProbasePayPage,
  ],
  imports: [
    IonicPageModule.forChild(ProbasePayPage),
  ],
})
export class ProbasePayPageModule {}
